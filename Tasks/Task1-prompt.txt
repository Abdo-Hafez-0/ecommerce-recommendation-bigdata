You are working on a Big Data project using MongoDB Atlas and Python (pymongo).

Dataset: Amazon Reviews dataset

Collections:

* users
* products
* reviews

Each review document contains:
overall, vote, verified, reviewTime, reviewerID, asin, style, reviewerName, reviewText, summary, unixReviewTime

Relationships:

* One-to-Many: Users → Reviews, Products → Reviews
* Many-to-Many: Users ↔ Products via Reviews
* One-to-One: User → Profile (embedded document)

---

⚠️ GENERAL RULES:

1. Use MongoDB Atlas connection
2. Use correct field names (no changes)
3. Write clean, commented code
4. Each task must include working examples
5. Code must be ready to integrate into one project

---

📦 OUTPUT REQUIRED:

* Python code / notebook section
* Queries with explanations
* Clear examples for testing
-------------------------------------------------


YOUR TASK: CRUD Operations + GUI (Deferred)

PART 1 — CRUD:

1. CREATE:

* insert_one()
* insert_many()

2. READ:

* find()
* find_one()
* projection

3. UPDATE:

* update_one()
* update_many() using $set

4. DELETE:

* delete_one()
* delete_many()
* soft delete using "deleted_at"

---

PART 2 — GUI (IMPORTANT NOTE):

The GUI implementation is temporarily SUSPENDED.

* Do NOT start GUI now
* Focus only on completing CRUD operations
* GUI will be implemented AFTER:

  * Queries are finished
  * Aggregation is done
  * Database structure is stable

---

Goal:

* Build a complete CRUD module first
* Prepare functions that will later be used inside the GUI
